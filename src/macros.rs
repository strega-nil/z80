// NOTE(ubsan): figure out how to do this with features
 /*
macro_rules! debug {
  ($($args:expr),*$(,)*) => (
    println!($($args),*);
  )
}
// */
// /*
macro_rules! debug {
  ($($args:expr),*$(,)*) => (
  )
}
// */
